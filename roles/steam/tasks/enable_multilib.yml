---

- name: Enable multilib in /etc/pacman.conf
  ineinfile:
    dest: /etc/pacman.conf
    regexp: '^[multilib]\n#Include = /etc/pacman.d/mirrorlist$'
    line: '[multilib]\nInclude = /etc/pacman.d/mirrorlist'